## What I Learn
set
A much better data type to store values that aren't duplicates than list (for member testing). 
-> A set look up its element by using hash, a list require a linear scan, it will take the item and put it inside hash function to calculate its key (memory address) and that key will be directed to the spot. 